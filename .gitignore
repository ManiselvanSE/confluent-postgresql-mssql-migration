# Sensitive configuration (passwords, secrets)
*password*
*secret*
.env
.env.local
.manienv
*.pem

# Local overrides with credentials
debezium-postgres-config-local.json
debezium-postgres-config.json
client.properties.local
config/replicator-connector.json

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs (if committed from EC2)
*.log
